  purge_keep_days: 60
  db_url: mysql://hass:hass@core-mariadb/homeassistant?charset=utf8
  include:
    domains:
      - person
      - sensor
      - binary_sensor
      - switch
      - light
      - alarm_control_panel
      - input_text
      - input_boolean
    entities: 
#Объем БД
      - sensor.maria_db_size